<!DOCTYPE html>
<html>

<head>
  <title>{{title}}</title>
  <link rel='stylesheet' href='/stylesheets/style.css' />
  <meta name="viewport" content="width=device-width, initial-scale=1">
</head>

<body {{#if isDarkmode}}class="darkmode" {{/if}}>
  <div class="page-grid">
    <header>
      <span>{{title}}</span>
      <a id="toggle-theme" class="link-button action" href="/tasks/toggle/theme_dark_{{invertedisDarkmode}}">{{#if isDarkmode}}Helles
        design{{else}}Dunkles
        design{{/if}}</a>
    </header>
    <div class="page-content">
      {{{body}}}
    </div>
    <footer><span>Copyright © 2020 Moritz Schiesser</span></footer>
  </div>
</body>

</html>